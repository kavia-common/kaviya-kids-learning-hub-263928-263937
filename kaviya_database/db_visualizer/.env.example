# Example environment configuration for simple-db-viewer
# Copy to .env and adjust as needed.

# MongoDB connection string (includes authSource=admin)
MONGODB_URI="mongodb://appuser:dbuser123@localhost:5000/myapp?authSource=admin"

# Optional: Override port the viewer listens on (default 3000)
# PORT=3000
